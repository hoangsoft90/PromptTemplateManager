# Session Notes — 2026-08-13

Ghi chú phiên làm việc. Xem thêm `working.md` (nhật ký ngắn) + `openspec/` (spec chi tiết).

## Việc đã làm trong phiên này

### Privacy Policy & Store Prep
- Tạo `PRIVACY_POLICY.md` (tiếng Anh, email `haibasoftware@gmail.com`) — khai báo theo đúng thực tế code: 100% on-device (SQLite local, không account/backend/cloud), ads AdMob **non-personalized only** (`requestNonPersonalizedAdsOnly: true` cả 4 format), import/export chỉ do user kích hoạt, không analytics/crash reporting.
- Host lên GitHub Pages: https://hoangsoft90.github.io/PromptTemplateManager/ (orphan branch `gh-pages` + `index.html` tự chứa policy, HTTP 200 verified, Pages source = gh-pages).

### Review toàn bộ code (tìm sai UI/logic/crash)
- **🔴 HIGH — Search gãy hoàn toàn trên native**: `db/promptRepository.sqlite.ts:121` — `ESCAPE '\\\\'` trong template literal JS = 2 ký tự backslash trong SQL, mà SQLite yêu cầu ESCAPE char đúng 1 ký tự → mọi search trên Android/iOS throw "ESCAPE expression must be a single character". Đã xác minh bằng `node:sqlite` thật. **Vì sao test không bắt được**: toàn bộ test dùng web backend (`promptRepository.web.ts` — `String.includes`, không có ESCAPE clause). **⚠️ CHƯA FIX — đưa vào backlog.**
- MEDIUM: `usePrompts.ts` effect search thiếu `catch` (unhandled rejection nếu search throw); tour step "tabs" spotlight bao cả màn hình (ref bọc cả `<Tabs>`); Toast dùng chung 1 Animated.Value.
- Đã kiểm tra OK: ads pipeline (singleton preload, app-open throttle 3 phút, không stack fullscreen), variable engine (escape/masking), import/export conflict table, db migrate, onboarding state machine, không leak secret.

### Navigation rà soát + fix
- Fix Settings deep-link dead end: thêm `hasHistory` + back pill "← Back to library" (đối xứng Detail).
- Dọn import `router` thừa trong `fill.tsx`/`edit.tsx`; `favorites.tsx` đổi `router.navigate('/(tabs)')` → `router.navigate('/')`.
- Xác nhận: mọi nút quay về qua `safeBack()` (`lib/navigation.ts`), không còn `router.back()` trực tiếp; web export không warning; expo-haptics có web impl.

### Ads cooldown test
- Thêm `__tests__/adsCooldown.test.ts` (5 test): skip 30s sau cold start, max 1 app open/3 phút, không stack lên interstitial vừa hiện, failed show không tiêu throttle, chưa load → false. Mock AdMob phát LOADED/CLOSED + fake timers. **Full suite 176/176, tsc clean.**

### Build & Release (GH Actions, không EAS token)
- `git remote` trỏ `github.com/hoangsoft90/PromptTemplateManager`, commit gốc push thành công.
- Fix `.gitignore` (bản cũ là Flutter, mất các mục Expo: `/android`, `/ios`, `.expo/`, `dist/`, `apk/`, `*.jks`...) → `android/`/`dist/`/`apk/`/`.expo/` được ignore lại.
- Tạo skill `.agents/skills/gh-actions-apk-build/SKILL.md` (build APK/AAB qua GH Actions, repo + token dùng qua env — **không nhúng token**).
- **Build APK**: workflow `build-apk.yml` chạy thành công ×2 (~20-23 phút/lần, artifact tải được).
- **Build AAB signed (Play Store)**: tạo keystore `prompt-template-manager.keystore` (alias `prompttemplate`, RSA 2048, 10000 ngày) — **gitignored, mật khẩu giữ ngoài repo**; set 4 GitHub Secrets (`KEYSTORE_BASE64`, 2 passwords, alias); config plugin `plugins/withReleaseSigning.js` (no-op khi không có env, verified prebuild local) đăng ký `app.json`; workflow `build-aab.yml` (`bundleRelease`) — **đang chạy lần đầu** (run `31660191578`, ~20 phút).
- Play Store cách A: dùng **Play App Signing** (upload key = keystore này).

### Store listing
- Tạo `chplay.md`: App Name `Prompt Template Manager`, short/full description ASO, category Productivity + 5 tags, 4 ý tưởng screenshot + feature graphic 1024×500 + icon 512×512, Play Console content checklist (kèm privacy URL gh-pages). **Chưa commit.**

### OpenSpec
- Cập nhật MVP change (commit `d6f1367`): tasks 6.5 đổi EAS → GH Actions (verified), thêm section 9 (signed AAB workflow, signing plugin, keystore Secrets, privacy gh-pages, chplay.md draft, Play Store upload pending); design.md mitigation EAS → GH Actions gradle. `openspec validate --all` pass.

## Quyết định thiết kế chốt trong phiên
- Build production: **GH Actions gradle (không EAS token)** — APK debug-signed cho test, AAB ký keystore thật cho Play Store.
- Keystore dùng làm **upload key** cho Play App Signing (cách A).
- `.project/` là thư mục quy trình AI (file này + `ai-rules.md`) — track trên git.

## Việc còn dang dở (xem `working.md` backlog)
1. **🔴 Fix bug SQL ESCAPE** (`db/promptRepository.sqlite.ts:121`) + thêm test bắt lỗi này + bọc catch `usePrompts` — chưa ai xử lý.
2. AAB build `31660191578` đang chạy — verify artifact + nộp Play Console (Play App Signing).
3. Commit `chplay.md` (chưa push).
4. Trước khi publish: `TEST_ADS = false` + thay 4 iOS placeholder unit IDs.
5. `SectionHeader` dead code — chờ quyết định xóa/giữ.
